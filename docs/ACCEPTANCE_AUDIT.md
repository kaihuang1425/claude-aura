# Eight-theme acceptance audit

This matrix records the evidence available through 2026-08-09. **Proven** means the
implementation and a directly relevant check are present. **Partial** means
source or automated evidence exists but the required live behavior has not yet
been observed. **Pending** means the final artifact or runtime evidence still
needs to be produced. Current visual acceptance evidence consists of
interactive signed-in production `claude.ai` runs, localized Aura Studio
walkthroughs in a real WebView2 host, and explicitly identified whole-window
Aura captures. Fixture renders, offline UI boards, comparison screenshots, and
golden images were retired and deleted on 2026-07-20; they cannot satisfy any
current or future acceptance gate. The queue records HUMAN CHECKPOINT D as
completed. The 2026-08-08 owner waiver permits later source/mechanical work to
continue past WO-27 and HUMAN CHECKPOINT CODE, but it does not mark either Code
gate passed or authorize a final release claim.

| Requirement | Evidence | Status |
| --- | --- | --- |
| Default plus seven named themes, with stable IDs and one registry | `themes/registry.json`; eight canonical `themes/<id>.json` files; exact-order registry test in `tests/run-tests.mjs` | Proven |
| Central semantic-token architecture and stable root theme attribute | `scripts/theme-core.mjs`; `assets/base.css`; `assets/theme-variants.css`; `assets/renderer-inject.js`; semantic-completeness and payload tests | Proven |
| Distinct typography, shapes, effects, controls, cards, composer, navigation, status, and theme variants | Per-theme typography/shape/effect data; centralized component recipes in `assets/theme-variants.css`; payload compilation checks and 2026-07-21 whole-window live-Aura Light/Dark captures for all eight themes | Proven for the WO-17 theme matrix and user-approved HUMAN CHECKPOINT C parity review |
| Theme gallery with name, subtitle, card image, swatches, selected state, keyboard access, immediate application, adjustable card framing, and appearance mode | Aura Studio gallery in `studio/app.js`; strict allowlisted bridge and isolated local virtual hosts in `windows/aura-ui.ps1`; seven packaged uncropped PNG masters with frozen source hashes and registry starting frames; polished 3:2 card windows; shared pointer-drag/native-range framing dialog up to 600% card zoom; native System/Light/Dark radio group; Windows assertions in `tests/run-tests.mjs`; completed HUMAN CHECKPOINT D walkthrough | Proven for the WO-18 boundary and recorded Checkpoint D walkthrough |
| Casual and advanced users can create and edit a safe custom theme without losing rapid changes | Visual-first Create route; default Quick customize with localized names, essential palette/style/background/artwork, visible privacy and validation; Advanced state/framing inspector with exact numeric inputs; persistent Back/status/Cancel/Save header; stable layer IDs; serialized/coalesced page mutations; independently validated one-to-sixteen-change `apply-theme-patch`; one-revision/one-Undo core tests; explicit hidden-route isolation; renderer-selected Standard/Wide framing; clipped live-main geometry; queued/debounced/in-flight unsaved-state coverage; exact bridge/source contracts in `tests/studio-editor.test.mjs` and lifecycle coverage in `tests/user-kits.test.mjs` | Proven for the WO-18 boundary: the mechanical gates pass and the queue records the localized/keyboard/install/restart/delete walkthrough complete at HUMAN CHECKPOINT D. |
| Selected and last-valid themes safely style the Studio shell | Canonical `studioStyleFromTheme` projection in `scripts/theme-core/studio.mjs`; exact dual-mode colour/alpha and approved font/radius/blur/shadow DTO validation in the Node core, PowerShell bridge, and Studio page; CSS-variable application in `studio/app.js` over the centralized defaults in `studio/styles.css`; contrast-safe material compositing; last-valid editor-state source; Default/System Original-look reset; fixed accent foreground; forced-colors override; artwork, personal-wallpaper, CSS, URL, and path exclusions; contract coverage in `tests/studio-editor.test.mjs` and `tests/platform.test.mjs` | Proven for WO-18: the all-eight Light/Dark/System, valid/invalid draft, restart, Original-look, forced-colors/keyboard, and media-exclusion review is recorded complete at HUMAN CHECKPOINT D. |
| Every permanent theme has a distinct, safe Studio chrome profile | Immutable generated-theme snapshot in `studio/app.js`; eight fixed `data-studio-shell` branches in `studio/styles.css`; shared profile variables consumed by gallery/editor surfaces; neutral user-theme and active-draft boundary; pairwise non-colour signature, exact-ID, no-media/path/raw-colour, reduced-motion, increased-contrast, and forced-colours assertions in `tests/studio-editor.test.mjs` | Proven for WO-18: mechanical coverage and the installed all-eight Gallery/settings mode cycle were completed at HUMAN CHECKPOINT D. |
| Theme-aware floating Studio launcher and running-app identity with safe fallback | Eight distinct authored 1254×1254 transparent sources; deterministic 96×96 PNG plus per-theme nine-frame 16/20/24/32/40/48/64/128/256 ICO; one active identity shared by the main and Studio windows, taskbar, notification area, Studio rail/favicon, permanently circular 48×48 floating launcher, and exact four owned Aura/Studio Desktop/Start shortcuts; whole-button DPI-scaled 6 px drag threshold with an action menu on click or right-click; custom PNG-to-content-addressed-ICO derivation; Original-look Default fallback; strict path/dimension/alpha/budget checks | Proven for WO-18: the corrected assets pass current mechanical gates and their installed quality/interaction review is recorded complete at HUMAN CHECKPOINT D. |
| Persistent selection, immediate reapplication, legacy migration, safe Default fallback, and persisted framing | Registry aliases, atomic configuration handling, strict focal-point/zoom validation, corrupt-file preservation/recovery in `scripts/theme-core.mjs` and `scripts/theme-cli.mjs`; payload-returning init/set/recovery tests; Windows startup and navigation reapply flow; the 2026-07-18 Studio bridge walkthrough restored both card and background crop values after reload | Proven for storage, migration, recovery, framing, and Studio reload; a visible native close-and-reopen run in the production window remains pending |
| Phase 2 unified editor, responsive layout sets, loading-screen editor, and `.aura` package handoff | WO-20/21/25/26/28/22 source contracts; 205 tests across 28 suites at `26cb329`; 16-state payload cycle; PowerShell parse; all eight status-only asset audits | Partial — source and mechanical gates pass. HUMAN CHECKPOINT F was owner-skipped on 2026-08-09 and remains unpassed; no current-build installed interaction, accessibility, persistence, package round-trip, or whole-window actual-Aura visual approval is claimed. |
| Aura System/Light/Dark preference is persisted, reversible, and independent of Claude account settings | Strict `appearance` config/CLI validation and atomic writes; `CoreWebView2Profile.PreferredColorScheme` before first navigation and after live config changes; Aura-owned selected/effective-mode attributes with `matchMedia` listener cleanup; Original look returns the profile to Auto; no Claude class, `data-mode`, storage, or account mutation; executable and static regressions in `tests/run-tests.mjs`; actual Study Library light/dark Aura captures | Proven |
| English, Simplified Chinese, and Traditional Chinese metadata and Windows picker UI copy remain independent of theme styling | Localized registry fields; `windows/ui-copy.json`; native Studio framing labels/status/errors in `studio/app.js`; locale normalization and complete-key assertions in `tests/run-tests.mjs`; explicit UTF-8 loading in Windows PowerShell; zh-CN and zh-HKTW framing walkthroughs | Proven |
| Original Claude identity remains present and themeable | One repository-only normalized source per frozen built-in plus prompt/provenance record; exact supplied full-lockup reuse for Japanese Film Editorial and Japanese Idol; approved raster reuse for Korean Idol; newly authored Default, Korean Prestige, Cartoon Studio, Anime Twilight, and Study Library sources; separate Light/Dark full-wordmark PNGs under every `assets/theme-art/<id>/`; one decoded-only, pointer-inert renderer channel that preserves the native wrapper and uses native fallback for collapsed/undersized, ambiguous, failed, forced-colors, cleanup, SPA-remount, and Original-look states | Proven for the stable WO-18 surface; the expanded/collapsed Light/Dark and fallback review is recorded complete at HUMAN CHECKPOINT D. |
| Isolated, optional, pointer-inert renderer artwork; concept composites never become renderer UI | Isolated SVG/WebP renderer layers under `assets/theme-art/`; seven decorative uncropped Studio masters with live adjacent labels and separately stored framing; source-only alternate and authored launcher-source/prompt exclusion; asset policy tests; `NOTICE.md`; `THIRD_PARTY_NOTICES.md` | Proven |
| Cartoon Studio production artwork | Approved appearance-specific background and mascot WebPs under `assets/theme-art/cartoon-studio/`; source checksum freeze; status-only asset audit; exact recipe-layer assertions; Light/Dark payload compilation; actual Aura Light/Dark captures | Proven and approved at HUMAN CHECKPOINT C. The narrower 1280 x 720 stress case remains part of WO-16's broader sweep. |
| Anime Twilight production artwork | Approved cityscape WebP under `assets/theme-art/anime-twilight/`; source checksum freeze and exact prompt; status-only asset audit; exact recipe-layer assertions; Light/Dark payload compilation; actual Aura Light/Dark 1920 x 1080 captures | Proven and approved at HUMAN CHECKPOINT C. |
| Study Library production artwork | Approved appearance-specific paper backgrounds and alpha still-life WebP under `assets/theme-art/study-library/`; source checksum freeze; status-only asset audit; exact recipe/main-canvas anchor assertions; Light/Dark payload compilation; actual Aura Light/Dark whole-window captures under gitignored `dist/verify/live-aura/` | Proven. The user approved the Light/Dark Study Library composition on 2026-07-20; the live captures are the visual review evidence. |
| Japanese Idol production artwork | Light floral background and top-right new-chat portrait plus dedicated Dark new-chat/conversation WebPs under `assets/theme-art/kawaii-idol/`; source/runtime checksums recorded; status-only dimensional, alpha, raster, and payload-budget audit; exact four-layer assertions; superseding actual Aura Light/Dark new-chat/conversation whole-window revision captures under `dist/verify/live-aura/` | Proven and approved. The first Checkpoint C review rejected the Light framing; the user approved the revised 74%/660px top-right composition with Dark unchanged on 2026-07-21. The four production assets and recorded hashes are frozen. |
| Interface remains usable when artwork is absent or fails | Artwork resolution returns a non-blocking unavailable state; shared backgrounds and component styles do not depend on artwork; failure-path tests | Proven |
| Contrast, focus, readable sizes, high contrast, forced colors, and reduced motion | Contrast guardrail tests; focus/high-contrast/reduced-motion rules in `assets/base.css` and `assets/theme-variants.css`; native high-contrast handling in `windows/aura-ui.ps1`; 2026-07-18 Studio walkthroughs confirmed a visible keyboard ring | Partial — automated/source and Studio keyboard focus are covered; the live assistive-technology and OS-preference matrix remains pending |
| Responsive behavior at 1280×720, 1440×900, 1905×1026, 1920×1080, and high-DPI equivalent | Responsive CSS, artwork reduce/hide policy, DPI-aware native layout, normal-display whole-window matrix, 2560 × 1600 at 200% review, and the normal/fullscreen Checkpoint D walkthrough | Proven for the stable WO-18 responsive boundary |
| Theme switching is responsive, active artwork only is loaded, and decoration avoids layout shift or continuous motion | Active-theme-only artwork compilation; one-pass native save/startup payload path; cached image/art CSS values; direct-node observer; pointer-inert assets; reduced-motion tests; live Light/Dark matrix, context switching, and representative hover review | Proven for WO-17 switching/context/interaction scope; production paint timing remains a later performance observation |
| Theme-aware native Windows identity | Per-theme nine-frame built-in ICO loading; validated custom PNG-to-content-addressed-ICO conversion with native handle cleanup; coordinated main window, Studio window, taskbar, notification-area, and exact four owned shortcut updates on theme switch; complete-identity validation before the visible state commits; shell refresh; Original-look Default fallback | Proven for WO-18: current mechanical verification passes and the revised installed multi-theme identity review is recorded complete at HUMAN CHECKPOINT D. |
| Pinned taskbar identity and live-window grouping | One shared explicit `ClaudeAura` AppUserModelID for the process and native installer shortcuts; Aura's bounded, top-level, reparse-safe discovery of pinned copies; exact installed PowerShell/script/argument validation; foreign-identity exclusion; atomic icon plus identity update and rollback; executable real-`.lnk` coverage in `tests/studio-editor.test.mjs` | Proven for WO-18: executable shortcut coverage and the installed multi-theme grouping review were completed at HUMAN CHECKPOINT D. |
| Existing WebView2 navigation, sign-in, Claude loading, restore, background, and Desktop app behavior remain intact | Navigation-ID-correlated main-document lifecycle and DOM-ready fail-open reveal in `windows/aura-ui.ps1`; real WebView2 popup/opener semantics for allowlisted Claude and sign-in origins; executable stale-completion, DOM-ready cancellation, genuine-failure, success, popup-origin, external-HTTPS, and unsafe-scheme regressions in `tests/studio-editor.test.mjs`; exact background cover/framing geometry shared by Studio and renderer; marker-owned content-addressed Studio background cache; same-size/same-timestamp, oversized-replacement, and junction-alias regressions; 2026-07-18 signed-in loading plus live theme/background/enabled interactions; user-confirmed successful first sign-in on the installed 2026-07-23 build | Partial — the recurrent first-sign-in blank-page path and framing contract are proven; Desktop-app launch and restore edge cases remain source-verified only |
| Source-only Claude Desktop permanent-theme presentation | Exact signed-build/AUMID/HWND binding; external rail and click-through shell; exact-HWND Windows Graphics Capture with 13 semantic colors plus two translucency roles; appearance artwork, identities, radius/blur and launcher shape/type tokens; one non-persisting 8-theme by 2-appearance live cycle with 16 GPU starts, 17 frames, zero visual-channel failures, and automatic Original-look cleanup; focused 14/14 checks | Proven for the local experiment on signed MSIX `1.26832.0.0` and the available 200% display. This is external GPU/owned-surface approximation, not renderer/DOM styling, cross-display evidence, installer inclusion, release approval, or formal actual-Aura WebView2 acceptance. |
| CLI-first styled Aura Code View and full-workspace routes | Stable WO-27 and HUMAN CHECKPOINT CODE require an affirmative support contract plus the complete consent, process, privacy, local read/write, reconnect, all-theme, accessibility, and handoff matrix. Safe implemented slices are official terminal-theme export, `desktop-guidance-only`, and an explicit source-checkout outer-shell experiment. | Pending / NO-GO — stable embedded Code and its local bridge are not implemented or release-authorized. Native Claude Desktop remains stock. Terminal, guidance, and the source experiment cannot substitute for the live checkpoint. |
| No Aura-owned runtime font CDN, npm dependency, account sync, subscription, or analytics system | `package.json`; system font stacks; dependency statement in `docs/IMPLEMENTATION_REPORT.md`; vendored WebView2 notices. Anthropic may independently require an eligible account, authentication method, organization policy, and network access for Claude and Remote Control. | Proven for Aura's own dependency boundary; WO-27 must capability-probe and truthfully report official Remote Control eligibility without claiming Aura can bypass it. |
| Installation is non-elevated, allowlisted, documented, and safely reversible | Native Inno Setup 7 source under `installer/`; deterministic wizard art; exact Node.js/WebView2 readiness and local-path disclosure; transaction-correlated finalization; retryable rollback; staged artifact verification and rollback-safe promotion in `scripts/build-installer.mjs`; guarded native/legacy uninstall surfaces; explicit opt-in removal of settings/WebView data; `docs/WINDOWS_INSTALLER.md`; `README.md`; `SECURITY.md` | Partial — the unsigned developer executable compiles and its real English welcome/trust/tasks/ready/cancel flow was inspected without a UAC prompt. A CA-trusted signed build, clean-VM fresh/update/repair/uninstall matrix, and live installed Aura launch remain required before public distribution. |
| Tests, parsing, payload cycle, Studio metadata build, and release exclusions | Clean-clone `npm run check` 170/170; `npm run verify:cycle` 17/17 with payload-only Light/Dark compilation for all eight themes; all eight status-only asset audits; exact nine-frame per-theme ICO validation; zero parse errors across all 15 Windows PowerShell files; fresh-state verifier; built-in and user-theme terminal exports; `scripts/build-release.mjs` exclusion policy | Proven for current committed non-visual checks; this does not close HUMAN CHECKPOINT CODE or the post-cue source-experiment review |
| Final release archive matches the completed source and includes final evidence | Versioned release builder and SHA-256 generation in `scripts/build-release.mjs`; explicit archive allowlist and source-only exclusions | Partial / NO-GO — WO-16 can produce a hash-matched release candidate, but final public release is refused while WO-27 and HUMAN CHECKPOINT CODE remain open. The authoritative candidate digest stays in the generated sidecar. |
| Whole-window screenshots of all eight themes and live theme-switch verification | 2026-07-21 local gitignored evidence: all eight themes in Light/Dark, Original look, Studio selection/navigation, representative live hover/selected state, Korean Idol new-chat/conversation contexts, and normal/200% DPI Aura identity | Proven for WO-17 and approved at HUMAN CHECKPOINT C on 2026-07-21 |

## Deliverable evidence

The architecture summary, change-set inventory, canonical IDs, dependency
rationale, asset licensing, unsafe-reference list, recorded automated results,
and remaining limitations are maintained in
`docs/IMPLEMENTATION_REPORT.md`. The implementation is on
`work/full-theme-system`.

As of 2026-07-20, the old reconstructed-harness captures and claims are retired
and do not satisfy any live acceptance gate. Signed-in production loading and
all eight theme selections were observed. The Aura Studio framing revision was
exercised at its exact product viewport with pointer drag, native keyboard
controls,
host-confirmed save, reload persistence, both Chinese locales, and no fresh-run
console warnings or errors. The user approved HUMAN CHECKPOINT B on 2026-07-19.
Study Library additionally has approved actual-Aura light/dark evidence and
checksum-frozen production art from WO-12.

WO-17 adds a complete 16-state Light/Dark matrix of the actual Aura window on
live `claude.ai`, plus Original look, Studio, normal/high-DPI identity,
Korean Idol conversation, and live hover evidence. The images remain private
under `dist/verify/live-aura/`; their machine-readable inventory and hashes are
in `wo17-self-capture-manifest.json`. The context probe reported exactly one
`data-claude-aura-sidebar`, conversation context, zero new-chat prompt markers,
and four conversation-scoped artwork layers. Studio's real **Back to Claude
Aura** action foregrounded the existing Aura window. The 200% review used an
actual 2560 × 1600 display and the original 1920 × 1080 topology was restored.

Remaining production-only checks are targeted: console/network instrumentation,
full assistive-technology coverage, the broader normal/fullscreen stress
matrix, title-bar platform variance, the themed launcher's cold-start
visibility and whole-button hover/click/drag/menu interaction, live uninstall,
and the ordered WO-16 sweep. The user approved
HUMAN CHECKPOINT C on 2026-07-21.
